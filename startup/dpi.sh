#!/usr/bin/env sh
# set monitor position and size
rogdrv-config dpi -d 800 --preset 0 && rogdrv-config dpi -d 800 --preset 1 && rogdrv-config dpi -d 800 --preset 2 && rogdrv-config dpi -d 800 --preset 3

